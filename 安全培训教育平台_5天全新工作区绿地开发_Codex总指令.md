# 物化院有限公司安全培训教育平台 V0.1
## 5 天全新工作区绿地开发总指令（可整段发送给 Codex）

> 本文件以《开发需求问答》为产品依据。
>
> 本项目采用 **greenfield / 从零开发**：当前在 Codex 中打开的新工作区是唯一项目根目录。不得读取、复制、迁移、改造或兼容任何旧项目代码、旧数据库迁移、旧分支、旧测试、旧任务包、旧交接文档或旧构建产物。
>
> 本项目只有一个连续开发任务和 Day 1—Day 5 五个日里程碑，不使用 D00—D40、G0—G6、R01—R03，也不建立几十个任务包。

---

# 一、可直接发送给 Codex 的总指令

```text
现在开始执行【物化院有限公司安全培训教育平台 V0.1｜5 天全新工作区绿地开发】。

这是一个从零创建的新项目。当前在 Codex 中打开的目录就是唯一 PROJECT_ROOT。你必须在该目录内完成 Web 管理后台、后端 API、数据库、微信小程序和部署文件。

====================
一、绝对边界：禁止使用旧代码
====================

1. 严禁访问、读取、搜索、复制、导入或参考 PROJECT_ROOT 以外的任何旧安全生产平台目录。
2. 尤其不得访问或使用下列旧目录及其 Git 历史：
   - E:\codex\safety
   - E:\codex\safety-web
   - E:\codex\safety-mini
   - 任何名称包含 old、legacy、backup、archive、training-module 的旧仓库或备份目录
3. 严禁复制旧项目的：
   - 源代码；
   - package.json、锁文件和工程配置；
   - SQL、migration、schema、seed；
   - 页面、组件、接口、RPC、RLS；
   - 测试、测试数据和测试报告；
   - AGENTS.md、任务包、交接记录和需求追踪矩阵；
   - Git commit、branch、tag、patch 或 diff。
4. 不做旧系统兼容，不做旧数据迁移，不保留旧编号，不修复旧 migration，不建立 legacy adapter。
5. 本项目没有需要导入的真实历史业务数据。数据库直接创建 V0.1 初始结构。
6. 如果当前新工作区为空，立即初始化项目；如果已有本次新项目文件，只能读取和修改当前工作区内的文件。
7. 除安装依赖、调用系统工具和读取官方依赖缓存外，所有项目读写必须限制在 PROJECT_ROOT 内。
8. 第一次输出只允许报告：PROJECT_ROOT、是否为空、Node/pnpm/Docker/Git 是否可用。不得花时间寻找旧项目。

====================
二、项目目标与上线定义
====================

在 5 个开发日内，从零完成一个可部署、可供第一批真实人员使用的安全培训教育 MVP。

核心闭环固定为：

管理员建立人员和项目
→ 管理员上传课件、建立试卷和培训模板
→ 管理员选择人员/组织/项目并下发培训
→ 人员在微信小程序查看待办、学习、考试
→ 考试通过后本人在小程序手写签字一次
→ 项目入场教育由项目管理员确认“已到场、已接受现场交底”
→ 管理员在 Web 查询完成情况、催办、打印和导出记录。

产品定位：
- 安全培训工具；
- 项目培训管理辅助工具；
- 培训档案和检查资料生成工具。

第一版不是：
- 自动判断可否上岗的准入系统；
- 特种作业证照系统；
- 自动复训规则引擎；
- 电子通行证或二维码核验系统；
- 综合安全生产管理平台。

任何页面、接口和报表都不得显示或计算：
- 可上岗；
- 禁止上岗；
- 资格有效/失效；
- 准入凭证；
- 人员通行二维码。

只展示培训事实和下一步动作：
- 待学习；
- 学习中；
- 待考试；
- 需补学；
- 已锁定；
- 待本人签字；
- 待项目确认；
- 已完成；
- 已取消。

====================
三、技术方案：按此直接初始化
====================

为保证 5 天交付，使用一个 Git 仓库、一个后端服务、一个 PostgreSQL 数据库、一个管理后台和一个原生微信小程序。

固定技术栈：

1. 包管理与仓库
   - Node.js 22 LTS；
   - pnpm workspace；
   - API、Web 和共享契约使用 TypeScript strict；
   - 单仓库，不创建 worktree，不拆多个长期分支；
   - 默认分支 main。

2. 后端 API
   - Fastify + TypeScript；
   - Prisma ORM + PostgreSQL；
   - Zod 请求校验；
   - Argon2 密码哈希；
   - jose/JWT；
   - REST JSON API；
   - 所有客户端只能通过 API 访问数据，禁止浏览器和小程序直连数据库。

3. Web 管理后台
   - React + Vite + TypeScript；
   - Ant Design；
   - React Router；
   - TanStack Query；
   - 中文界面；
   - 生产构建后由 Fastify 同域提供静态文件，减少部署服务数量。

4. 微信小程序
   - 原生微信小程序 JavaScript（ES6 + JSDoc）；
   - 不增加 TypeScript 转译链，不使用 Taro、uni-app 或跨端框架；
   - 使用微信原生页面、组件、Canvas 和 web-view；
   - API 地址统一由环境配置提供；
   - AppSecret 永远只在后端。

5. 数据库与文件
   - PostgreSQL 15+；
   - Prisma migration 只从 V0.1 初始迁移开始；
   - 文件默认保存到 API 服务器的持久化目录 var/uploads；
   - Docker/生产部署必须把该目录挂载为持久卷；
   - 照片、签字和 HTML 课件不提供永久公开目录；
   - 文件读取必须经过带权限检查的 API；
   - HTML 课件通过短时签名 URL 打开受控 viewer 页面。

6. 本地与部署
   - docker-compose.yml 至少提供 PostgreSQL；
   - API、Web、数据库迁移和管理员初始化提供一键命令；
   - 提供 Dockerfile；
   - 不绑定某一家云平台；
   - 生产只要求一个 HTTPS 域名指向 API/Web 服务，另配置微信 request 和业务域名。

建议目录必须保持简洁：

PROJECT_ROOT/
  apps/
    api/
    admin/
    miniprogram/
  packages/
    contracts/
  prisma/
    schema.prisma
    migrations/
    seed.ts
  var/
    uploads/
  docs/
  docker-compose.yml
  Dockerfile
  pnpm-workspace.yaml
  package.json
  .env.example
  README.md

生产环境不允许启用 Mock。开发环境可以提供最小假账号和模拟微信登录，但必须由 NODE_ENV=development 严格控制。

====================
四、认证与账号绑定
====================

1. Web 管理员
   - 用户名 + 密码登录；
   - 密码使用 Argon2；
   - 登录后使用 HttpOnly、SameSite Cookie；
   - 提供 CLI/seed 命令创建首个 company_admin；
   - 生产 seed 不写入固定默认密码。

2. 微信小程序人员
   - wx.login 获取临时代码；
   - 后端使用 WECHAT_APP_ID、WECHAT_APP_SECRET 调用微信会话接口；
   - 后端建立 openid 与人员账号绑定；
   - 使用微信 getPhoneNumber 能力时，只把一次性 code 发送给后端，由后端换取手机号；
   - 已登记且唯一匹配的手机号自动绑定人员档案；
   - 未匹配或多条匹配时创建待审核绑定申请；
   - 无法使用手机号能力时允许本人填写资料并提交人工审核，但不得自动获得已登记人员身份；
   - 换手机号、换微信、重复档案、资料错误、离职停用由管理员处理，同时保留本人发起变更申请入口。

3. 会话安全
   - 小程序使用短期 access token + 可撤销 refresh session；
   - code、session_key、AppSecret、access token 不写普通日志；
   - 账号停用或角色撤销后新请求立即失效；
   - 生产环境缺少微信配置时明确报错，禁止静默使用 Mock。

====================
五、人员、组织、角色与项目
====================

人员类型必须同时支持：
- employee：正式员工；
- contractor：有单位外协人员；
- temporary_individual：无单位临时个人。

人员必填字段：
- 姓名；
- 手机号；
- 所属组织或责任部门；
- 身份证号码；
- 个人照片。

人员能力：
- 正式员工支持 CSV/XLSX 批量导入和管理员逐人创建；
- 外协人员、临时个人支持管理员创建/导入；
- 外协人员、临时个人也可从小程序提交注册申请，经项目管理员审核；
- 不管理外协合同、资质、营业执照和单位历史版本；
- 只关联外协单位名称、人员、责任组织和项目；
- 身份证号使用 AES-256-GCM 应用层加密；
- 另存不可逆 hash 用于重复检查，另存 last4 用于脱敏展示；
- 普通列表、打印、CSV 和日志不得出现完整身份证号码；
- 不建设完整个人资料批量导出。

角色：
- company_admin：全公司；
- org_admin：指定经营实体或部门范围；
- project_admin：指定项目范围，合并原项目经理/安全员的系统操作入口；
- learner：本人。

权限规则：
- 同一账号可拥有多个角色和多个 scope；
- company_admin 可以设置 org_admin 和 project_admin；
- org_admin 可在自身组织范围内管理人员、账号、组织关系、项目关系、课件、培训和管理员；
- project_admin 只能管理授权项目；
- 取消“每个项目最多两名项目经理”限制；
- 任何管理员只能授予不高于自己数据范围的权限；
- learner 只能读取和操作本人数据；
- 权限统一在 Fastify preHandler/service 查询条件中实施，前端隐藏按钮不能替代服务端鉴权；
- 每个写操作记录操作者、时间、对象和动作。

项目：
- 一个项目只有一个管理责任实体；
- 状态只有 active、paused、ended；
- 同一人员可以同时参加多个项目；
- 项目成员主要由管理员创建或导入；
- 扫项目二维码申请不作为 V0.1 上线要求；
- 不联动旧项目月报或其他业务系统；
- paused：禁止新增项目成员和新项目培训，历史可查；
- ended：只读；
- paused 恢复 active 后，由 project_admin 手工发起重新现场交底确认，不自动生成复训。

====================
六、课件、模板与培训安排
====================

培训类型只实现：
- three_level：员工基础三级教育；
- project_induction：项目入场教育；
- routine：日常/年度培训；
- change_update：变化内容补充培训。

暂不实现：
- 专项培训；
- 爆破、电工、焊工、钻探规则；
- 证照；
- 返岗培训；
- 长期停工复训；
- 自动事件识别和复训规则。

课件类型只实现：
- rich_text：图文课件；
- single_html：单文件 HTML 交互课件。

课件规则：
- 管理员上传现成材料；
- 图文课件使用简单富文本编辑器，图片通过私有上传接口保存；
- HTML 只允许单个 .html 文件，限制大小并清理危险响应头；
- 不解析 Word/PDF，不建设课件生成器；
- company_admin 可直接发布；
- company_admin 可授权 org_admin/project_admin 发布其范围内课件；
- 取消送审、签发和多人审批；
- published 版本不得原地覆盖；
- 修改产生新 version；
- 已完成记录继续引用旧版本；
- 未完成任务默认继续旧版本，管理员可手工切换新版本。

培训模板：
- 模板由名称、培训类型、适用范围和课件顺序组成；
- 三级教育只做少量可编辑模板；
- 公司级、经营实体级、第三级内容在同一个任务清单内展示；
- 不逐级解锁；
- 所有必学内容完成后统一考试；
- employee 参加 three_level；
- contractor 和 temporary_individual 不参加 employee three_level；
- project_induction 必须绑定项目；
- routine、change_update 可以按组织或人员下发，不强制绑定项目；
- 项目通用课件可复用，项目模板可追加项目风险、作业环境和管理要求。

培训下发：
- 选择模板/课件；
- 选择人员、组织或项目；
- 设置截止时间；
- 选择固定试卷或随机组卷规则；
- 设置考试参数；
- 生成 training_batch 和逐人 assignment；
- 同一 batch + person 只能有一条有效 assignment；
- 支持撤销尚未完成的错误任务并保留日志。

自动下发只实现两种：
- 新 employee 状态变为 active 时，幂等生成 three_level 任务；
- 人员加入项目并审核通过时，幂等生成 project_induction 任务。

首次上线：
- 提供“一键为当前全部 active employee 生成三级教育任务”；
- 不录入旧纸质完成历史，按用户决定重新学习。

====================
七、学习、考试、补学和签字
====================

学习：
- 小程序首页第一屏只突出“我必须完成的任务”；
- 任务详情展示全部必学课件和当前下一步；
- 图文课件在小程序内展示；
- single_html 使用 web-view 打开短时签名 URL；
- 课件打开、到达末尾并主动点击“完成学习”后记录完成；
- 不做防挂机、随机确认题、最低有效学时和视频防拖动；
- 所有课件完成后进入待考试。

考试：
- 支持固定试卷；
- 支持从指定题库按题量简单随机抽题；
- 题型只做单选、多选、判断；
- 每次培训可独立设置题量、考试时长、合格分、最大次数；
- 系统参数保存公司默认值；
- 后端在开始考试时生成不可变试卷快照；
- 后端负责开始时间、截止时间、评分、次数和最终状态；
- 小程序自动保存答案；
- 退出或网络中断后恢复同一 attempt 和剩余时间；
- 最终交卷幂等；
- 不能把正确答案和评分规则提前返回客户端；
- 不接纸质试卷和扫描件。

补学与锁定：
- 不及格后 assignment 进入 remediation_required；
- 人员重新打开全部必学课件，并对每项点击“补学完成”后允许下一次考试；
- 达到最大次数后进入 locked；
- org_admin 可解锁本组织范围；
- project_admin 只可解锁本项目培训；
- 解锁记录原因、操作者和时间。

签字：
- 考试通过后显示完整培训记录预览；
- 学员在小程序 Canvas 上本人手写签字一次；
- 提交前可重写，正式提交后不得覆盖；
- 保存 PNG、assignment_id、person_id、记录摘要 hash、签字时间和设备基本信息；
- 签字文件私有；
- 签字后普通培训进入 completed；
- project_induction 签字后进入 confirmation_pending。

无手机或不会操作人员：
- 管理员可录入真实线下培训和考试结果；
- 仍要求人员本人完成签字；
- 第一版允许 project_admin 在自己的手机/平板打开“辅助签字”页面，由被培训人员当场本人手写签字；
- 辅助签字必须记录被签字人员、协助管理员、时间和 assignment，不允许管理员代签。

====================
八、项目确认、线下培训、提醒
====================

项目确认：
- 只对 project_induction 使用；
- project_admin 逐人确认“人员已到场、已接受现场交底”；
- 不采集照片；
- 不采集定位；
- 不扫码核验；
- 不生成资格或通行二维码；
- 确认后 assignment 进入 completed；
- 项目从 paused 恢复 active 后，可由 project_admin 手工建立一次重新确认批次；
- 重新确认不要求人员重学或重考，除非管理员另外下发培训。

线下集中培训：
- 使用同一 training_batch，source=offline；
- 记录培训名称、内容、日期、起止时间、实际学时、授课人、参加人员和附件；
- 每名人员生成独立 assignment/record；
- 管理员逐人录入学习完成、考试成绩和签字状态；
- 不上传纸质答卷；
- 不允许 learner 自己把线下记录改成完成；
- 所有录入保留来源、操作者和时间。

提醒：
- 小程序待办；
- 系统内消息中心；
- 管理员手工催办；
- 截止前一次提醒；
- 逾期按周汇总提醒；
- 不每日提醒；
- 不建设提醒处理闭环。

微信订阅消息：
- 只实现可插拔适配器和授权入口；
- 配置了已审核模板且用户授权时发送；
- 未配置、未授权、额度不足或发送失败时只保留系统内消息；
- 不作为 Day 5 上线阻塞；
- AppSecret 和 access token 只在后端。

====================
九、Web 页面和小程序页面
====================

Web 管理后台只保留 8 个一级菜单：
1. 首页
2. 人员与账号
3. 组织与项目
4. 课件与模板
5. 题库与试卷
6. 培训安排
7. 进度与记录
8. 报表与设置

不要为公司、经营实体和项目复制三套页面。使用统一页面，并根据登录人的 role + scope 过滤数据和操作。

Web 必须页面：
- 登录；
- 首页统计；
- 人员列表、编辑、导入、审核、停用和绑定处理；
- 组织和管理员授权；
- 项目和项目成员；
- 课件与版本；
- 培训模板；
- 题库、题目、固定试卷和随机规则；
- 培训创建、人员选择和下发；
- 进度列表、详情、催办、解锁和项目确认；
- 线下培训录入；
- 报表和系统默认考试参数。

小程序主 Tab 只保留：
1. 待办
2. 我的记录
3. 消息
4. 我的

普通人员页面：
- 微信登录；
- 手机号/档案绑定；
- 待人工审核；
- 必须完成的任务；
- 任务详情；
- 图文学习；
- HTML web-view；
- 考试与恢复；
- 补学；
- 手写签字；
- 本人记录和成绩；
- 资料/绑定变更申请。

拥有 project_admin 时，在“我的”中显示管理入口：
- 待审核申请；
- 项目成员；
- 未完成人员；
- 考试不合格/锁定人员；
- 催办；
- 解锁；
- 到场/现场交底确认；
- 重新确认；
- 辅助签字。

====================
十、报表与首页统计
====================

V0.1 必须提供以下 5 类报表：
1. 三级安全教育台账；
2. 三级安全教育记录卡；
3. 培训签到表；
4. 考试成绩单；
5. 年度培训统计。

实现方式：
- Web 查询页；
- 浏览器打印样式；
- CSV 导出；
- 不建设复杂 PDF 引擎；
- 使用真实人员、任务、课件版本、考试、签字和项目确认数据；
- 不写固定虚假课程；
- 默认脱敏手机号和身份证；
- 不提供完整个人资料批量导出。

首页统计：
- 应培训人数；
- 已完成人数；
- 未完成人数；
- 考试不合格人数；
- 已锁定人数；
- 管理员可以保存自己要显示/隐藏的统计卡片。

====================
十一、初始数据库对象
====================

从零建立一套清晰的 Prisma schema。对象可以按合理命名调整，但不得拆出多套平行业务模型。

至少包含：
- accounts：登录账号、状态；
- persons：人员档案、人员类型、手机号、身份证密文/hash/last4、照片；
- wechat_bindings：openid、unionid（可空）、账号绑定和历史；
- organizations：公司、经营实体、部门、外协单位；
- organization_memberships：人员组织关系；
- role_assignments：角色、scope_type、scope_id、有效状态；
- projects：项目、责任实体、状态；
- project_members：项目人员关系和审核状态；
- files：私有文件元数据；
- coursewares；
- courseware_versions；
- training_templates；
- training_template_items；
- question_banks；
- questions；
- exam_papers；
- exam_paper_items；
- training_batches；
- training_assignments；
- learning_progress；
- exam_attempts；
- exam_answers；
- signatures；
- project_confirmations；
- notifications；
- change_requests；
- audit_logs；
- user_preferences。

关键约束：
- 手机号标准化；
- active 人员手机号按合理范围去重；
- openid 在同一 AppID 下唯一；
- 同一 batch + person assignment 唯一；
- 同一 attempt 只能最终交卷一次；
- 正式签字不可 update/replace，只允许新增更正记录并保留原记录；
- project_admin 写操作必须匹配项目 scope；
- 自动下发使用唯一业务键，重复运行不重复创建；
- 课件版本一经用于已下发任务，不得原地修改；
- completed 记录不得由普通更新接口回退；
- 关键表具有 created_at、updated_at；
- 写入 audit_logs 的失败不能影响主业务事务，但关键高风险操作必须保留日志。

====================
十二、明确不开发的范围
====================

本次 5 天禁止开发：
- 旧项目兼容和旧数据迁移；
- D00—D40、G0—G6、R01—R03；
- Web/H5 员工端；
- 专项培训；
- 爆破、电工、焊工、钻探规则；
- 证照管理；
- 返岗培训、长期停工复训；
- 自动准入或上岗资格状态机；
- 可上岗/禁止上岗名单；
- 电子凭证；
- 二维码核验；
- 访客、公司领导、临时通行；
- 现场照片和定位；
- 离线学习、离线考试和离线同步；
- 多经营实体共同负责项目；
- 月报或其他旧系统联动；
- 外协合同和资质；
- Word/PDF 自动解析；
- 视频学习体系；
- 课件送审、签发和多人审批；
- 完整个人资料批量导出；
- 复杂流程编排器；
- 复杂数据大屏；
- 自动化 E2E 测试体系；
- 性能压测；
- 独立审查 Agent；
- 大量测试报告、追踪矩阵和交接文档。

不得因为“以后可能需要”而预先建设上述功能。只保留正常扩展所需的干净字段和模块边界。

====================
十三、5 天连续开发安排
====================

不要把每天再拆成多个正式任务包。按下面顺序连续开发；只在当天结束时提交一次。

Day 1｜工程、数据库、账号、人员、组织和项目
- 初始化 pnpm monorepo、Git、Docker、PostgreSQL、Prisma；
- 创建初始 schema 和 migration；
- 创建 Fastify API 和统一错误格式；
- 创建 React 管理后台骨架；
- 创建原生微信小程序骨架；
- 完成管理员用户名密码登录；
- 完成 company_admin/org_admin/project_admin/learner 权限基础；
- 完成人员、组织、项目、项目成员和角色管理；
- 完成 CSV/XLSX 人员导入；
- 完成微信登录、手机号自动匹配和待审核绑定流程；
- 完成私有文件上传基础；
- 创建首个管理员初始化命令。

Day 1 必须能演示：
- 新数据库一键创建；
- company_admin 登录；
- 创建组织、项目、人员和管理员；
- 导入人员；
- 小程序微信用户进入绑定/待审核状态；
- learner 无法读取他人档案。

Day 2｜课件、模板、题库、培训下发和考试后端
- 图文和单 HTML 课件上传、发布和版本；
- 三级教育、项目入场、日常/年度、变化内容模板；
- 题库、单选/多选/判断；
- 固定试卷和简单随机组卷；
- 培训 batch、assignment 和选择人员/组织/项目；
- 新 employee 和项目成员审核后的幂等自动下发；
- 考试开始、快照、计时、答题保存、恢复、交卷、评分；
- 补学、最大次数锁定和范围内解锁；
- Web 对应管理页面。

Day 2 必须能演示：
- 管理员发布课件；
- 创建培训模板和试卷；
- 选人并下发；
- 自动生成三级教育/项目入场任务；
- API 完成一次考试全流程。

Day 3｜小程序学习、考试、签字和本人记录
- 首页“我必须完成的任务”；
- 任务详情；
- 图文课件；
- HTML web-view；
- 学习进度；
- 考试、自动保存、恢复、交卷；
- 补学和锁定提示；
- 完整记录预览；
- Canvas 手写签字；
- 我的记录和考试成绩；
- 消息中心；
- 资料变更申请。

Day 3 必须能演示：
- 一个 learner 登录绑定；
- 打开所有课件；
- 完成考试；
- 中断后继续同一次考试；
- 不及格后补学再考；
- 通过后本人签字；
- 在本人记录中查看结果。

Day 4｜项目管理、线下培训、催办和报表
- project_admin 小程序管理入口；
- 审核注册/项目加入；
- 查看未完成、不合格和锁定人员；
- 手工催办；
- 范围内考试解锁；
- 项目入场到场/现场交底确认；
- 恢复后的重新确认；
- 辅助签字；
- Web 线下集中培训和逐人记录；
- 截止前一次提醒和逾期周汇总；
- 五类报表、浏览器打印和 CSV；
- 首页统计卡片和显示偏好。

Day 4 必须能演示：
- project_admin 在小程序完成审核、催办、解锁和确认；
- Web 实时看到同一人员状态；
- 新建一批线下培训并形成逐人记录；
- 打印三级教育记录卡、签到表和成绩单。

Day 5｜只做上线阻塞修复、部署和发布
- 完成 production build；
- 配置生产环境变量；
- 执行全新生产数据库 migration；
- 创建首个 company_admin；
- 配置持久化 uploads 卷；
- 部署 API + 管理后台；
- 设置 HTTPS、CORS/Cookie 和微信合法域名；
- 构建微信小程序；
- 条件具备时上传体验版/候选版并提交平台审核；
- 修复主流程、越权、数据保存、评分、签字和构建阻塞；
- 输出短版管理员手册和部署说明；
- 创建 v0.1.0 tag。

Day 5 禁止进行：
- 非阻塞 UI 美化；
- 代码覆盖率；
- 大规模重构；
- 性能优化；
- 旧系统比较；
- 第二轮需求扩张。

====================
十四、测试与验收：只保留最小上线检查
====================

用户明确不需要大量测试。不要建立单元测试、组件测试、自动化 E2E、测试矩阵、性能测试或多轮审查流程。

开发中只运行：
- TypeScript 类型检查；
- 受修改影响的 build；
- Prisma schema/migration 校验；
- 必要的 API 手工请求。

Day 5 只做一次最小上线 smoke：

1. 空 PostgreSQL 执行 prisma migrate deploy 成功；
2. API production build 成功；
3. Admin production build 成功；
4. 微信开发者工具能编译小程序；
5. company_admin 手工完成：建组织→建项目→导入人员→发课→建试卷→下发→看进度→打印；
6. learner 手工完成：登录绑定→学习→考试→补学/重考→签字→看记录；
7. project_admin 手工完成：审核→催办→解锁→到场/交底确认；
8. learner 读取他人资料被拒绝；
9. org_admin 跨组织操作被拒绝；
10. project_admin 跨项目操作被拒绝；
11. 小程序包、前端包、日志和仓库不含 AppSecret、数据库密码、JWT secret、session_key 或完整身份证样例。

只修复下列阻塞：
- 无法构建或启动；
- 主流程无法完成；
- 数据无法保存；
- 考试计时/评分明显错误；
- 正式签字或记录无法形成；
- 明显越权；
- 敏感信息泄露；
- 数据库 migration 无法执行。

其余问题写入 docs/mvp-post-launch.md，不延误 V0.1。

====================
十五、最低安全要求
====================

即使快速开发，也禁止：
- AppSecret、数据库密码、JWT secret 进入 Git、Web 包或小程序包；
- 客户端直接连接数据库；
- 前端直接指定角色、考试通过、任务完成或签字完成；
- learner 查看他人身份证、照片、成绩、签字或培训记录；
- org_admin/project_admin 越过 scope；
- 永久公开照片、签字和 HTML 文件目录；
- 完整身份证写入日志、URL、CSV 或浏览器缓存；
- 物理删除已完成的培训、考试、签字和项目确认；
- production 静默回退到 Mock；
- 用硬编码管理员身份绕过权限。

敏感字段：
- 身份证 AES-256-GCM 加密；
- 环境变量 FIELD_ENCRYPTION_KEY 必须是生产独立密钥；
- API 默认返回 masked 值；
- 单人编辑场景读取完整值也必须有明确管理员权限和审计；
- 照片、签字、HTML 下载使用短时 token，且在服务端再次检查当前权限。

====================
十六、环境变量
====================

至少提供 .env.example，只有变量名和说明，不写真实值：

NODE_ENV=
PORT=
PUBLIC_BASE_URL=
DATABASE_URL=
COOKIE_SECRET=
JWT_SECRET=
FIELD_ENCRYPTION_KEY=
UPLOAD_ROOT=
UPLOAD_SIGNING_SECRET=
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_SUBSCRIBE_TEMPLATE_TASK=
WECHAT_SUBSCRIBE_TEMPLATE_DUE=
ADMIN_BOOTSTRAP_USERNAME=
ADMIN_BOOTSTRAP_PASSWORD=

生产 ADMIN_BOOTSTRAP_PASSWORD 只用于一次性初始化，创建成功后必须提示删除该变量或改密。

====================
十七、交付文件
====================

最终必须交付：
- 可运行的单仓库源码；
- Prisma 初始 schema 和 migration；
- database seed/bootstrap 命令；
- Web 管理后台；
- Fastify API；
- 微信小程序；
- Dockerfile 和 docker-compose.yml；
- .env.example；
- 人员 XLSX/CSV 导入模板；
- 五类报表；
- README.md：本地启动；
- docs/mvp-admin-guide.md：管理员简明操作；
- docs/mvp-deploy.md：生产部署、HTTPS、微信配置、备份和回退；
- docs/mvp-smoke-checklist.md：唯一的上线检查表；
- docs/mvp-post-launch.md：非阻塞问题；
- v0.1.0 Git tag。

不要生成：
- 需求追踪矩阵；
- 数十份 handoff；
- D 编号状态表；
- 测试覆盖率报告；
- 架构论文式文档。

====================
十八、Codex 执行纪律
====================

1. 立即运行 pwd/Get-Location，确认当前 PROJECT_ROOT；不要询问旧工作区路径。
2. 若未初始化 Git，执行 git init，创建 main；不要连接或拉取旧远端。
3. 先创建完整目录和可启动骨架，再按 Day 1—Day 5 连续实现。
4. 不要先花半天写方案；本指令已经是方案。首轮现状输出不超过 10 行，随后立即写代码。
5. 产品细节出现小歧义时，选择最简单、可维护、符合本指令的实现并继续，不停下来等待确认。
6. 只有缺少真实 AppID/AppSecret、域名、服务器权限等外部值时，使用环境变量占位，并在 Day 5 人工项中一次性列出。
7. 每天结束最多一次汇报，格式固定为：
   - 今日完成；
   - 当前能演示的完整链路；
   - 真实阻塞；
   - 下一步；
   - commit hash。
8. 每天最多一个 commit；Day 5 创建 v0.1.0。未配置远端时不要反复询问 push。
9. 不创建第二套后端，不把业务规则复制到小程序；小程序只展示和提交动作。
10. 不为将来范围提前搭复杂框架，不顺手开发明确排除功能。

现在立即开始 Day 1。

第一步仅输出不超过 10 行：
- 当前 PROJECT_ROOT；
- 当前目录是否为空；
- Git/Node/pnpm/Docker 可用状态；
- 将创建的顶层目录；
- 是否存在阻止写代码的真实问题。

然后无需等待确认，直接初始化项目、数据库和三端骨架，并连续完成 Day 1 可演示链路。
```

---

# 二、使用说明

1. 在 Codex 中打开你新建的工作区根目录。
2. 新建一个 Codex 会话。
3. 将本文件“一、可直接发送给 Codex 的总指令”中的整个代码块发送给 Codex。
4. 不再同时发送旧任务包、旧 AGENTS.md、旧交接记录或旧数据库说明。
5. Codex 只需要知道当前打开的新工作区，不需要知道旧工作区路径。

# 三、5 天交付边界说明

本指令已经按“从零开发”重新压缩。5 天代码目标是：完成新数据库、新 API、新管理后台和新小程序的核心培训闭环，并形成可部署候选版本。

微信小程序能否在第 5 天对所有用户正式可见，还取决于 AppID、主体类目、合法域名、隐私协议、业务域名和微信平台审核。Codex 必须在第 5 天完成代码、构建和可提交版本，但不得虚构平台已经审核通过。
