# 野外项目月报测试站逐页验收记录（2026-09-22）

## 部署前基线（以下是旧站点观察，非最终版本）

- 地址：`https://test.safety.sx.cn/safety` → 正常入口“野外项目报送” → `/monthly-reports`。
- 已登录身份：`company_admin_test`，页面显示公司管理员、未设置部门。
- 测试站页面资源：`/assets/index-ASdFtJ6s.js`；本地未提交月报改动的构建资源为 `index-BpI5ekXb.js`。本次检查的是**测试站当前已部署版本**，不能作为本地新版 M 系列改造的验收。
- 仅做查看、月份切换和列表筛选；没有新增、修改、提交、退回或删除数据。
- 截图视口为当前 Codex 内置浏览器约 1079×911；未做 1440×1000 Figma 同视口像素验收。

## 页面与实际操作

| 页面/操作 | 真实入口与结果 | 证据 | 结论 |
| --- | --- | --- | --- |
| 公司报送查看 | 从安全管理平台正常入口进入；2026-09 显示 1 个报送实体、0 个已报送、1 个未报送、0 个项目 | `01a-manage-viewport.png` | 只读入口可用；没有可验收的已提交数据 |
| 经营实体详情 | 点击“测试经营实体 → 查看”，抽屉显示未报送、项目数 0、空项目表 | `02-manage-entity-detail.png` | 查看动作可用；批次和项目详情未验证 |
| 上月数据 | 月份切到 2026-08；仍为 1 个未报送实体、0 个项目 | `05-manage-previous-month.png` | 月份切换生效；上月完整报送链未验证 |
| 状态筛选 | “已提交”为无数据；“未报送”显示测试经营实体 | `06-submitted-filter-empty.png`、`07-unsubmitted-filter.png` | 两个筛选状态与当前数据一致 |
| 完工项目 | 0 个项目、0 个部门，列表空；年度显示为 `2,026` | `03a-completed-viewport.png` | 空状态可见；年度千分位格式错误 |
| 报送配置 | 截止日为 5；仅“测试经营实体”被勾选；项目类型列表为空，报送字段存在 | `04a-config-top-viewport.png`、`04b-config-fields-viewport.png` | 只读展示可见；未测试保存或权限校验 |

页面顶部在当前视口出现文字被固定顶栏遮挡/残留字符的现象，见首屏和字段区截图。需在部署目标版本后再次核对，不能直接判定本地新版也有相同问题。

## 未跑通的关键链路

当前账号没有经营实体“我的报送”入口，且测试站 2026-08、2026-09 均无项目草稿或已提交批次。因此以下项目**未验证**，不是通过：项目草稿与上月带入、保存与历史、无项目预检、整批提交、重复提交幂等、公司退回、重提、作废后重填、批次详情和导出内容。

本地新版尚未部署测试站。要验证 `docs/monthly-report-figma-alignment.md` 所列 M01–M15，需先更新测试站至确定的本轮代码版本，再用归属“测试经营实体”的填报账号和隔离测试项目完成写入链路；公司管理员账号用于复核及退回。不得用当前空列表或静态检查替代上述真实流程。

## 部署后复核（2026-09-22）

- 新月报前后端由完整提交 `ee7bdca8843a433b9b880fe1e6c098bd0f483892` 归档构建；测试站 API 镜像 `test-ee7bdca` 健康。随后前端页签可见性修补到 `216323216d3bb0ea7fb18feb104d238341c4fd29`，测试站 Web 镜像 `test-2163232`。未部署生产。
- 测试数据库部署前留有 `test-pre-ee7bdca-20260922-132907.dump` 备份；没有执行迁移。`/api/health` 返回 `ok`，页面新资源包含“经营实体整批提交核对”“无需逐批确认”。
- 已登录公司管理员在旧入口 `/monthly-reports` 刷新后自动到 `/monthly-reports/manage`。2026-09 显示应报主体 1、已提交待查看 0、待补齐 1，测试经营实体未报送且项目数 0。`final-M09-review.png` 为最终前端修复后的首屏。
- 点击“查看状态”进入实体批次查看：未报送、0 个项目，项目表空；返回公司月报正常。`post-M10-empty-batch.png` 为第一次部署的相同业务视图。
- “无项目确认”标签能切换到独立空表；没有真实无项目批次可复核。见 `post-M11-no-project-empty.png`。
- 完工项目能进入筛选、导出入口和空状态，当前筛选 0 项；见 `post-M13-completed-empty.png`。旧版 `2,026` 年份格式在新版月份控件中未再出现。
- 报送配置的时限、主体、类型、字段页签均能打开。测试经营实体已纳入，其他展示实体未纳入；类型列表为空，字段有现有配置。见 `post-M12-config-deadline.png`、`post-M12-config-entities.png`、`post-M12-config-test-entity.png`、`post-M12-config-types-empty.png`、`post-M12-config-fields.png`。未点击启停/保存。
- 首次部署截图发现选中标签的前景色与背景同色；按浏览器计算样式定位通用壳层规则覆盖，修复并再部署。最终实测“经营实体批次”前景 `rgb(36,91,197)`、透明背景，文字可见（`final-M09-review.png`）。
- 当前公司管理员仍能看见左侧“经营实体月报”入口，但点击后显示“无权访问此月报页面”（`post-M02-permission-denied.png`）。服务端未越权，属于菜单权限呈现差异，尚待 UI 收口。

### 本次验收判定与待办

**部分完成，不能称全流程通过。** 公司侧导航、查询空状态、批次查看、配置只读和权限拒绝做了真实浏览器检查；当前测试站没有项目类型、测试项目、草稿或已提交批次，且浏览器身份仅为公司管理员。未验证经营实体草稿→上月带入→整批提交→公司退回→重提，以及幂等、作废、导出内容与历史保护。下一步须先用测试经营实体填报账号在测试站登录，并准备隔离测试项目与项目类型；再由公司管理员账号核对退回。不要在正式业务数据上制造测试记录。

## 经营实体填报账号续验

- 用户切换为工程物探所的野外项目报送人员后，从真实“安全生产管理平台 → 野外项目报送”入口进入 `/monthly-reports/mine`；页面可访问，经营实体下拉只显示“工程物探所”。见 `entity-empty-workspace.png`。
- 选择工程物探所、月份 2026-09 后，服务端预检显示本月无在建或暂停项目，同时明确阻断“该经营实体未启用月报；该月份尚未开放或已经锁定”。整批提交入口不可用。
- 点击“本月无野外项目”仅打开阻断弹窗，弹窗再次显示“服务端核对未通过”与同一原因；未提交任何无项目确认，也未写入项目草稿。见 `entity-preflight-blocked.png`。
- 将月份改为 2026-08 后，仍无项目，服务端给出相同的未启用/月度未开放或锁定阻断；不能仅通过选择上月绕过配置。
- 这证明当前账号入口与服务端阻断反馈可见，**不证明**草稿、整批提交、退回、重提或幂等通过。继续正向流程需公司管理员在测试站为适当的隔离测试经营实体启用月报并开放测试月份，且准备隔离在建/暂停项目及项目类型；不能仅凭填报账号已登录跳过这些前置条件。

## 开放后再次续验

- 用户开放填报后刷新测试站，工程物探所 / 2026-09 的“未启用、月份未开放”阻断消失；可进入“经营实体整批提交核对”。服务端显示应报项目 0、草稿 0、待完成 0，并称该月无项目。**未点击最终提交**，以免无项目记录占用后续项目流程测试。
- 当前填报账号通过正常入口进入“人员与组织管理 → 项目与成员”，页面显示“没有可查看的项目”，没有“新建项目”按钮；见 `entity-projects-empty-no-create.png`。代码中的项目新建权限仅授予公司管理员或管理该经营实体的组织管理员，野外项目报送人员不能靠报送页面自行创建主档。
- 下一步需使用有项目创建权限的测试站账号，经正常“项目与成员 → 新建项目”建立明确标为流程测试的工程物探所在建项目；再切回填报账号进行草稿、整批提交及公司退回验收。不得绕过业务权限直接写测试数据库并把结果称为真实流程通过。
