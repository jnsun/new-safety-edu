# 应收账款管理迁移批准记录

日期：2026-09-16

验收代码提交：`ae237bc579e5d327e52d6d4c366614d2c2cfbb37`

批准结论：`APPROVED FOR LOCAL MERGE / CUTOVER RUNBOOK AUTHORIZED`

## 批准依据

- Node.js 22.23.2 与 PostgreSQL 客户端/服务端 16.15 已在运行簿入口及嵌套包命令中同时验证。
- 三个专用数据库从空库执行迁移；主库种子重复执行保持 30 个部门、82 个字典项。
- 第二次完整 FINAL 从迁移到 E2E 连续执行，最终输出 `TARGET_FINAL_RESULT=PASS`。
- 正式容量为 50,000 台账、500,000 明细、250 页、50,000 行 XLSX，四个要求索引全部命中，清理为零残留。
- 完整 E2E 为七会话、5 条台账、5 行导出、22 条审计，清理为零残留。
- 七角色浏览器边界、active 入口、390×844 窄屏、独立财务部门、逐部门读写、独立 view-all、公司管理员默认隔离、私有文件和服务端权威金额均已有验收与整分支复审证据。

## 历史失败披露

- 早期 Node 24/PostgreSQL 17 有序门曾因测试 baseline 包装器失败后续跑，未被计为 FINAL PASS。
- 目标运行时首次尝试包含两个 wrapper/runtime 预检失败；随后第一次完整目标运行在 E2E 专库缺少生产字典种子时失败。这些证据均保留。
- 经用户明确批准，修正运行簿并重新创建三个空库后，第二次完整 FINAL 才获得连续 PASS。

## 批准边界与停止条件

- 本记录批准代码本地合并，并批准操作人进入 `CUTOVER.md` 的备份、不可变部署、受控开放和观察步骤；不宣称生产部署或切换已完成。
- Windows 上无法用 POSIX `0600` 证明附件权限。开放流量前必须验证 uploads NTFS ACL 仅允许服务账号、Administrators 和 SYSTEM；验证失败必须停止。
- 切换前备份恢复演练、旧系统写入冻结、负责人配置复核、受控发布后检查和观察期仍是强制条件；任一失败均按切换手册停止并执行获批处置。
