# 金额明细权威且正式财务事实不可删除

合同编号必填并由数据库保证全公司唯一；开票金额和到账金额分别以有效开票、回款明细合计为唯一真相源，只有汇总金额的历史数据转成明确的期初导入明细。正式台账、明细、附件和导入批次通过作废、冲销或回滚标记更正而不物理删除，关键财务写入、导出下载和授权恢复均形成永久审计。Excel 导入先完整校验和预览，再在一个事务中提交；并发更新检测到版本变化时拒绝覆盖。财务附件统一使用主系统私有文件服务及其 10MB、PDF/PNG/JPG/WebP/XLSX、哈希校验和下载鉴权标准。
