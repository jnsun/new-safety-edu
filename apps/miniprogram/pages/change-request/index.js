const api = require('../../utils/api')
Page({ data: { type: 'profile_change', requests: [], busy: false }, onShow() { this.load() }, chooseType(e) { this.setData({ type: e.detail.value }) }, async load() { try { this.setData({ requests: await api.request('/api/me/change-requests') }); } catch (_) {} }, async submit(e) { this.setData({ busy: true }); try { const values = e.detail.value; await api.request('/api/me/change-requests', 'POST', { type: this.data.type, name: values.name || undefined, phone: values.phone || undefined, reason: values.reason }); wx.showToast({ title: '已提交审核' }); await this.load(); } catch (error) { wx.showToast({ title: error.message, icon: 'none' }); } finally { this.setData({ busy: false }); } } })
