const api = require('../../utils/api')
Page({ data: { canManage: false }, async onShow() { try { const me = await api.request('/api/auth/me'); this.setData({ canManage: me.roles.some((role) => role.role !== 'learner') }); } catch (_) {} }, async subscribe() { try { const config = await api.request('/api/wechat/subscription-config'); if (!config.templateIds.length) return wx.showToast({ title: '订阅消息尚未配置', icon: 'none' }); const statuses = await new Promise((resolve, reject) => wx.requestSubscribeMessage({ tmplIds: config.templateIds, success: resolve, fail: reject })); await api.request('/api/wechat/subscription-consent', 'POST', { statuses }); wx.showToast({ title: '提醒设置已保存' }); } catch (error) { wx.showToast({ title: error.message || '未完成授权', icon: 'none' }); } }, changeRequest() { wx.navigateTo({ url: '/pages/change-request/index' }) }, manage() { wx.navigateTo({ url: '/pages/management/index' }) } })
